<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Java selenium (爬虫) 控制已打开的浏览器</title>
    <url>/2021/03/05/2021-03-05-01/</url>
    <content><![CDATA[<p>最近在做 selenium 爬虫相关的工作，起初使用selenium 时，每次运行时都会打开一个新的Chorme 浏览器，这就导致我们在之后的操作会丢失数据，还得继续从头再执行。对于这一问题，我们可以利用Chrome DevTools协议（此处针对的是Chorme浏览器），它允许客户检查和调试Chrome浏览器。</p>
<h4 id="操作步骤"><a href="#操作步骤" class="headerlink" title="操作步骤"></a>操作步骤</h4><p>1.找到我们Chorme 安装路径</p>
<a id="more"></a>

<p><img src="/2021/03/05/2021-03-05-01/2021-03-05-01%5Cimage-20210305141211981.png" alt></p>
<p><img src="/2021/03/05/2021-03-05-01/2021-03-05-01%5Cimage-20210305141411615.png" alt></p>
<p>2.在cmd 中输入   chrome.exe –remote-debugging-port=9222   （potr 可以自由指定） ，就会自动弹出一个浏览器</p>
<p><img src="/2021/03/05/2021-03-05-01/2021-03-05-01%5Cimage-20210305141558737.png" alt></p>
<p>3.相关程序</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">test01</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.setProperty(<span class="string">"webdriver.chrome.driver"</span>, <span class="string">"C:\\Program Files (x86)\\Google\\Chrome\\Application\\chromedriver.exe"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        ChromeOptions options = <span class="keyword">new</span> ChromeOptions();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="comment">//接管本地浏览器</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        options.setExperimentalOption(<span class="string">"debuggerAddress"</span>, <span class="string">"127.0.0.1:9222"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        WebDriver driver = <span class="keyword">new</span> ChromeDriver(options);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(driver.getPageSource());</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="comment">//driver.findElement(By.linkText("确认付款")).click();</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>那么再去执行这个程序之后，就会自动接管我们上步操作打开的浏览器了。在此处我碰到了一问题：无论我怎么配置都是无法实现接管，之后我又在options 中设置了几个参数 ，发现根本不生效，查阅治疗之后发现，我使用的selenium 版本过低导致，所以建议尽量使用3.5以上！！</p>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java基础 IO流</title>
    <url>/2020/08/15/IO/</url>
    <content><![CDATA[<h2 id="一、File类的使用"><a href="#一、File类的使用" class="headerlink" title="一、File类的使用"></a>一、File类的使用</h2><h3 id="1-如何创建File对象"><a href="#1-如何创建File对象" class="headerlink" title="1.如何创建File对象"></a>1.如何创建File对象</h3><ol>
<li><p>File类的一个对象，代表一个文件或文件目录；</p>
</li>
<li><p><strong>public File(String pathname)</strong></p>
<p>以pathname为路径创建File对象，可以是绝对路径或者相对路径，如果pathname是相对路径，则默认的当前路径在系统属性user.dir中存储</p>
<ul>
<li><strong>绝对路径</strong>：是一个固定的路径，从盘符开始</li>
<li><strong>相对路径</strong>：是相对于某个位置开始</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">File file=<span class="keyword">new</span> File(<span class="string">"hello.text1"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">File file1=<span class="keyword">new</span> File(<span class="string">"C:\\user\\123.text"</span>)</span></pre></td></tr></table></figure>
</li>
<li><p><strong>public File(String parent,String child)</strong></p>
<p>以parent为父路径，child为子路径创建File对象</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">File file2=<span class="keyword">new</span> File(<span class="string">"D:\\USER"</span>,<span class="string">"234.text"</span>)</span></pre></td></tr></table></figure>
</li>
<li><p><strong>public File(File parent,String child)</strong></p>
<p>根据一个父File对象和子文件路径创建File对象</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">File file3=<span class="keyword">new</span> File(file2,<span class="string">"345.text"</span>);</span></pre></td></tr></table></figure>
<a id="more"></a>
<h3 id="2-File类常用功能"><a href="#2-File类常用功能" class="headerlink" title="2.File类常用功能"></a>2.File类常用功能</h3></li>
</ol>
<h4 id="2-1-File类的获取功能"><a href="#2-1-File类的获取功能" class="headerlink" title="2.1 File类的获取功能"></a>2.1 File类的获取功能</h4><ul>
<li><p><strong>public String getAbsolutePath();</strong>  获取绝对路径</p>
</li>
<li><p><strong>public String getPath();</strong>  获取路径</p>
</li>
<li><p><strong>public String getName();</strong>  获取名称</p>
</li>
<li><p><strong>public String getParent();</strong>  获取上层文件目录。若无，返回null</p>
</li>
<li><p><strong>public long length();</strong>  获取文件长度（即：字节数）。不能获取目录长度</p>
</li>
<li><p><strong>public long lastModified();</strong>  获取左后一次的修改时间，毫秒值</p>
</li>
<li><p><strong>public String[ ] list();</strong>  获取指定目录下的所有文件或者文件目录的名称数组</p>
</li>
<li><p><strong>public File[ ] listFiles();</strong>  获取指定目录下的所有1文件或者文件目录的File 数组</p>
</li>
<li><p><strong>public boolean renameTo(File dest);</strong> 把文件重命名为指定的文件路径</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    File file1=<span class="keyword">new</span> File(<span class="string">"D:\\code\\123.text"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    File file2=<span class="keyword">new</span> File(<span class="string">"C:\\work\\234.text1"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">boolean</span> f=file1.renameTo(file2);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    System.out.println(f);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">//要想保证返回true,需要file1在硬盘中存在的，且file2不能再硬盘中存在</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="2-2-File类的判断功能"><a href="#2-2-File类的判断功能" class="headerlink" title="2.2 File类的判断功能"></a>2.2 File类的判断功能</h4><ul>
<li><strong>public boolean isDirectory();</strong>  判断是否是文件目录</li>
<li><strong>public boolean isFile();</strong>  判断是否是文件</li>
<li><strong>public boolean exists();</strong>  判断是否存在</li>
<li><strong>public boolean canRead();</strong>  判断是否可度</li>
<li><strong>public boolean canWrite();</strong>  判断是否可写</li>
<li><strong>public boolean isHidden();</strong>  判断是否隐藏</li>
</ul>
<h4 id="2-3File-类的创建功能"><a href="#2-3File-类的创建功能" class="headerlink" title="2.3File 类的创建功能"></a>2.3File 类的创建功能</h4><ul>
<li><p><strong>public boolean createNewFile();</strong> 创建文件。若文件存在，则不创建，返回false</p>
</li>
<li><p><strong>public boolean mkdir();</strong>  创建文件目录。如果此文件目录存在，就不创建了。如果此文件目录的上层文件目录不存，也不创建</p>
</li>
<li><p><strong>public boolean mkdirs();</strong>  创建文件目录。如果上层文件目录不存在，一并创建</p>
<p>注意事项：如果你创建文件或者文件目录没有写盘符路径，那么，默认在项目路径下</p>
</li>
</ul>
<h4 id="2-4File类的删除功能"><a href="#2-4File类的删除功能" class="headerlink" title="2.4File类的删除功能"></a>2.4File类的删除功能</h4><ul>
<li><p><strong>public boolean delete();</strong> 删除文件或者文件夹</p>
<p>删除注意事项：</p>
<p>java中的删除不走回收站，要删除一个文件目录，请注意该文件目录内不能包含文件或者文件目录</p>
</li>
</ul>
<h2 id="二、IO流"><a href="#二、IO流" class="headerlink" title="二、IO流"></a>二、IO流</h2><h3 id="1-IO流原理及流的分类"><a href="#1-IO流原理及流的分类" class="headerlink" title="1.IO流原理及流的分类"></a>1.IO流原理及流的分类</h3><ul>
<li><p>Java  IO 原理</p>
<p><strong>输入input</strong>：读取外部数据（磁盘、光盘等存储设备的数据）到程序（内存）中；</p>
<p><strong>输出output</strong>: 将程序（内存）数据输出到磁盘、光盘等存储设备中</p>
</li>
<li><p>流的分类：</p>
<ul>
<li><p>按操作<strong>数据单位</strong>不同分为： <strong>字节流（8bit）,字符流（16bit）</strong></p>
</li>
<li><p>按数据流的<strong>流向</strong>不同分为:  <strong>输入流</strong>，<strong>输出流</strong></p>
</li>
<li><p>按流的<strong>角色</strong>的不同分为： <strong>节点流</strong>，<strong>处理流</strong></p>
<table>
<thead>
<tr>
<th align="center">字节流</th>
<th align="center">（抽象基类）</th>
<th align="center">字符流</th>
</tr>
</thead>
<tbody><tr>
<td align="center">InputStream</td>
<td align="center">输入流</td>
<td align="center">Reader</td>
</tr>
<tr>
<td align="center">OutputStream</td>
<td align="center">输出流</td>
<td align="center">writer</td>
</tr>
</tbody></table>
</li>
</ul>
</li>
</ul>
<h3 id="2-流的体系"><a href="#2-流的体系" class="headerlink" title="2.流的体系"></a>2.流的体系</h3><p><img src="/2020/08/15/IO/IO%5CIO_01.png" alt></p>
<h3 id="3-流的使用"><a href="#3-流的使用" class="headerlink" title="3.流的使用"></a>3.流的使用</h3><ul>
<li>字符流</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//FileReader</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="comment">//1.实例File类对象，指明要操作的文件</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        FileReader fileReader = <span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            File file = <span class="keyword">new</span> File(<span class="string">"D:\\Code\\Test\\StudyJava\\helloword.txt"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="comment">//2.提供具体的流</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            fileReader = <span class="keyword">new</span> FileReader(file);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="comment">//3.数据的读入</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="comment">//read();返回读入的一个字符，如果达到文件末尾，返回-1</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">char</span>[] cbuf = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">5</span>];</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">int</span> len;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">while</span> ((len = fileReader.read(cbuf)) != -<span class="number">1</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; len; i++) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                    System.out.print(cbuf[i]);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="comment">//4.关闭流操作</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="keyword">if</span> (fileReader != <span class="keyword">null</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">                    fileReader.close();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//FileWriter	</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">	<span class="comment">//1.提供File类对象</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">       File file1 = <span class="keyword">new</span> File(<span class="string">"D:\\Code\\Test\\StudyJava\\helloword1.txt"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">       FileWriter fileWriter = <span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="comment">//2.提供流</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">           fileWriter = <span class="keyword">new</span> FileWriter(file1);</span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="comment">//3.写出操作</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">           fileWriter.write(<span class="string">"I have a dream!"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">       &#125; <span class="keyword">catch</span> (IOException e) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">           e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">       &#125; <span class="keyword">finally</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="keyword">if</span> (fileWriter != <span class="keyword">null</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">               <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                   <span class="comment">//4.关闭资源</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                   fileWriter.close();</span></pre></td></tr><tr><td class="code"><pre><span class="line">               &#125; <span class="keyword">catch</span> (IOException e) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                   e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">               &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">           &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       &#125;</span></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        FileWriter fileWriter = <span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        FileReader fileReader = <span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            File file = <span class="keyword">new</span> File(<span class="string">"D:\\Code\\Test\\StudyJava\\helloword.txt"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            File file1 = <span class="keyword">new</span> File(<span class="string">"D:\\Code\\Test\\StudyJava\\helloword2.txt"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">            fileReader = <span class="keyword">new</span> FileReader(file);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            fileWriter = <span class="keyword">new</span> FileWriter(file1);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">char</span>[] cbuf = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">5</span>];</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">int</span> len;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">while</span> ((len = fileReader.read(cbuf)) != -<span class="number">1</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                fileWriter.write(cbuf, <span class="number">0</span>, len);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="keyword">if</span> (fileReader != <span class="keyword">null</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">                    fileReader.close();</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="keyword">if</span> (fileWriter != <span class="keyword">null</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">                    fileWriter.close();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr></table></figure>

<ul>
<li>字节流</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr></table></figure>

<ul>
<li><p>缓冲流</p>
<p>作用：提高流的读写效率</p>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">//1.造文件</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	File file1=<span class="keyword">new</span> File(<span class="string">"C:\\USER\\123.png"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    File file2=<span class="keyword">new</span> File(<span class="string">"C:\\USER\\234.png"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    FileInputStream fileInputStream=<span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    FileOutputStream fileOutputStream=<span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    BufferedInputStream bufferedInputStream=<span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    BufferedOutputStream bufferedOutputStream=<span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">try</span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">     	<span class="comment">//2.造流</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">     	fileInputStream=<span class="keyword">new</span> FileInputStream(file1);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        fileOutputStream=<span class="keyword">new</span> FileOutputStream(file2);</span></pre></td></tr><tr><td class="code"><pre><span class="line">     	<span class="comment">//3.造缓冲流</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        bufferedInputStream=<span class="keyword">new</span> BufferedInputStream(fileInputStream);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        bufferedOutputStream=<span class="keyword">new</span> BufferedOutputStream(fileOutputStream);</span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="comment">//4.读写</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">byte</span>[] b=<span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">10</span>];</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">int</span> len;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">while</span>((len=bufferedInputStream.read(b)!=-<span class="number">1</span>)&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            bufferedOutputStream.write(b,<span class="number">0</span>,len);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;<span class="keyword">catch</span>(IOExecption e)&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;<span class="keyword">finally</span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">try</span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">          <span class="comment">//5.关闭 要求先关闭外层，在关闭内层</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="keyword">if</span>(bufferedInputStream!=<span class="keyword">null</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">               bufferedInputStream.close();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">if</span>(bufferedOutputStream!=<span class="keyword">null</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">            	bufferedOutputStream.close();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;<span class="keyword">catch</span>(IOException e)&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    </span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<ul>
<li><p>转换流</p>
<ul>
<li><p>供了在字节流和字符流之间的转换</p>
</li>
<li><p><strong>InputStreamReader:</strong> 将一个字节的输入流转换为字符的输入流</p>
</li>
<li><p><strong>OutputStreamWriter:</strong> 将一个字符的输出流站换为字节的输出流</p>
</li>
<li><p>字节流中的数据都是字符时，转成字符流操作更高效</p>
</li>
</ul>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//InputStreamReader</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        FileInputStream fileInputStream = <span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        InputStreamReader inputStreamReader = <span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            File file = <span class="keyword">new</span> File(<span class="string">"helloword.txt"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            fileInputStream = <span class="keyword">new</span> FileInputStream(file);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            inputStreamReader = <span class="keyword">new</span> InputStreamReader(fileInputStream);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">char</span>[] c = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">10</span>];</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">int</span> len;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">while</span> ((len = inputStreamReader.read(c)) != -<span class="number">1</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                String s = <span class="keyword">new</span> String(c, <span class="number">0</span>, len);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                System.out.println(s);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">if</span> (fileInputStream != <span class="keyword">null</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                    fileInputStream.close();</span></pre></td></tr><tr><td class="code"><pre><span class="line">                &#125; <span class="keyword">catch</span> (IOException e) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                    e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">                &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       FileInputStream fileInputStream = <span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       InputStreamReader inputStreamReader = <span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       OutputStreamWriter outputStreamWriter = <span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       FileOutputStream fileOutputStream = <span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">           File file = <span class="keyword">new</span> File(<span class="string">"helloword1.txt"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">           File file1 = <span class="keyword">new</span> File(<span class="string">"helloword3.txt"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">           fileInputStream = <span class="keyword">new</span> FileInputStream(file);</span></pre></td></tr><tr><td class="code"><pre><span class="line">           fileOutputStream = <span class="keyword">new</span> FileOutputStream(file1);</span></pre></td></tr><tr><td class="code"><pre><span class="line">           inputStreamReader = <span class="keyword">new</span> InputStreamReader(fileInputStream);</span></pre></td></tr><tr><td class="code"><pre><span class="line">           outputStreamWriter = <span class="keyword">new</span> OutputStreamWriter(fileOutputStream);</span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="keyword">char</span>[] c = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">10</span>];</span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="keyword">int</span> len;</span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="keyword">while</span> ((len = inputStreamReader.read(c)) != -<span class="number">1</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">               outputStreamWriter.write(c,<span class="number">0</span>, len);</span></pre></td></tr><tr><td class="code"><pre><span class="line">           &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">       &#125; <span class="keyword">catch</span> (Exception e) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">           e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">       &#125; <span class="keyword">finally</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">               <span class="keyword">if</span> (fileInputStream != <span class="keyword">null</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                   fileInputStream.close();</span></pre></td></tr><tr><td class="code"><pre><span class="line">               &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">               <span class="keyword">if</span> (fileOutputStream != <span class="keyword">null</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                   fileOutputStream.close();</span></pre></td></tr><tr><td class="code"><pre><span class="line">               &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">           &#125; <span class="keyword">catch</span> (Exception e) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">               e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">           &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">       &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">   &#125;</span></pre></td></tr></table></figure>

<ul>
<li><p>标准 输入、输出流（了解）</p>
<p>System.in : 标准的输入流，默认从键盘输入</p>
<p>System.out :  标准的输出流，默认从控制台输出</p>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        InputStreamReader inputStreamReader = <span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        BufferedReader bufferedReader = <span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            inputStreamReader = <span class="keyword">new</span> InputStreamReader(System.in);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            bufferedReader = <span class="keyword">new</span> BufferedReader(inputStreamReader);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                System.out.println(<span class="string">"请输入字符串："</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                String data = bufferedReader.readLine();</span></pre></td></tr><tr><td class="code"><pre><span class="line">                data = data.toUpperCase();</span></pre></td></tr><tr><td class="code"><pre><span class="line">                System.out.println(data);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                bufferedReader.close();</span></pre></td></tr><tr><td class="code"><pre><span class="line">                inputStreamReader.close();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr></table></figure>

<ul>
<li><p>打印流（了解）</p>
<p>PrintStream </p>
<p>PrintWriter</p>
</li>
<li><p>数据流（了解）</p>
</li>
<li><p>对象流</p>
<p>ObjectInputStream </p>
<p>ObjectOutputStream</p>
<p>用于存储和读取基本数据类型数据或对象的处理流</p>
<p><strong>序列化 ：</strong> 用ObjectOutputStream 类保存基本数据类型或对象机制</p>
<p><strong>反序列化 ：</strong> 用ObjectInputStream 类保存基本数据类型或对象机制</p>
<p>ObjectOutputSteam 和 ObjectInputStream 不能序列化static 和 transient修饰的成员变量</p>
<p><strong>对象序列化机制</strong>：允许把内存中的Java对象转换成平台无关的二进制流，从而允许把这种二进制流持久地保存在磁盘上，或通过网络将这种二进制流传输到另一个网络节点。当其他程序获取了这种二进制流，就可以恢复成原来的Java对象</p>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        FileOutputStream fileOutputStream=<span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        ObjectOutputStream objectOutputStream=<span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">             fileOutputStream=<span class="keyword">new</span> FileOutputStream(<span class="keyword">new</span> File(<span class="string">"obi.dat"</span>));</span></pre></td></tr><tr><td class="code"><pre><span class="line">             objectOutputStream=<span class="keyword">new</span> ObjectOutputStream(fileOutputStream);</span></pre></td></tr><tr><td class="code"><pre><span class="line">             objectOutputStream.writeObject(<span class="keyword">new</span> String(<span class="string">"我爱你"</span>));</span></pre></td></tr><tr><td class="code"><pre><span class="line">             objectOutputStream.flush();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;<span class="keyword">catch</span> (Exception e)&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;<span class="keyword">finally</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                objectOutputStream.close();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       FileInputStream fileInputStream=<span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       ObjectInputStream objectInputStream=<span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="keyword">try</span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            fileInputStream=<span class="keyword">new</span> FileInputStream(<span class="keyword">new</span> File(<span class="string">"obi.dat"</span>));</span></pre></td></tr><tr><td class="code"><pre><span class="line">            objectInputStream=<span class="keyword">new</span> ObjectInputStream(fileInputStream);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            Object o=  objectInputStream.readObject();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            String s=(String) o;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            System.out.println(s);</span></pre></td></tr><tr><td class="code"><pre><span class="line">       &#125;<span class="keyword">catch</span> (IOException | ClassNotFoundException e)&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">           e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">       &#125;<span class="keyword">finally</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">               fileInputStream.close();</span></pre></td></tr><tr><td class="code"><pre><span class="line">           &#125; <span class="keyword">catch</span> (IOException e) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">               e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">           &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr></table></figure>

<ul>
<li><p>RandomAccessFile</p>
<ul>
<li>直接继承了java.lang.Object类，实现了DataInput和DataOutput 接口</li>
</ul>
</li>
<li><p>既可以作为输入流，也可以作为输出流</p>
<ul>
<li>如果RandomAccessFile 作为输出流时，写出到文件1如果不存在，则在执行过程中自动创建，如果写出的文件存在，则会对原有的文件内容进行覆盖</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java基础 集合框架</title>
    <url>/2020/08/04/%E9%9B%86%E5%90%88%E6%A1%86%E6%9E%B6/</url>
    <content><![CDATA[<h2 id="Java基础-集合框架"><a href="#Java基础-集合框架" class="headerlink" title="Java基础   集合框架"></a>Java基础   集合框架</h2><h2 id="一、Java-集合框架概述"><a href="#一、Java-集合框架概述" class="headerlink" title="一、Java 集合框架概述"></a>一、Java 集合框架概述</h2><ol>
<li>集合、数组都是对多个数据进行存储操作的结构、简称Java容器;</li>
</ol>
<ol start="2">
<li>数组在存储多个数据方面特点：<ul>
<li>一旦初始化后，其长度就确定了</li>
<li>数组一旦定义好，其元素类型也就确定了，我们也就只能操作指定类型的数据了</li>
</ul>
</li>
</ol>
<ol start="3">
<li>数组在存储多个数据方面的缺点：<ul>
<li>一旦初始化以后，其长度就不可以修改了</li>
<li>数组中提供的方法是有限的，对于添加、删除、插入等操作，非常不便，同时效率不高</li>
<li>获取数组中实际元素的个数的需求，数组没有现成的属性或方法</li>
<li>数组存储数据的特点：有序、可重复。对于无序，不可重复的需求，不能满足<a id="more"></a><h2 id="二、集合框架"><a href="#二、集合框架" class="headerlink" title="二、集合框架"></a>二、集合框架</h2></li>
</ul>
</li>
<li>Java 集合可分为Collection 和Map 两种体系<ul>
<li>Collection接口：单列数据，定义了存取一组对象的方法的集合<ul>
<li>List : 元素有序、可重复的集合<pre><code>ArrayList、LinkedList、Vector</code></pre></li>
<li>Set : 元素无序、不可重复的集合<pre><code>HashSet、LinkedSet、TreeSet</code></pre></li>
</ul>
</li>
<li>Map接口：双列数据、保存具有映射关系“key-value”的一对的数据<ul>
<li>HashMap、LinkedHashMap、TreeMap、Hashtable、Properties </li>
</ul>
</li>
</ul>
</li>
</ol>
<p><img src="/2020/08/04/%E9%9B%86%E5%90%88%E6%A1%86%E6%9E%B6/01.png" alt></p>
<h2 id="三、List-接口"><a href="#三、List-接口" class="headerlink" title="三、List 接口"></a>三、List 接口</h2><h4 id="1-ArrayList、LinkedList、Vector-异同点"><a href="#1-ArrayList、LinkedList、Vector-异同点" class="headerlink" title="1. ArrayList、LinkedList、Vector  异同点"></a>1. ArrayList、LinkedList、Vector  异同点</h4><p>​    相同点：三个类都是实现了List接口、存储数据特点相同：存储有序、可重复的数据</p>
<p>​    不同点:   1. <strong>ArrayList</strong> :  作为List接口的主要实现类，<strong>线程不安全，效率高</strong>，底层使用Obiect[ ] elementData 存                        储;</p>
<p>​                    2.<strong>LinkedList</strong> : 对于频繁的插入，删除操作，使用此效率会比ArrayList 高，底层使用双向链表存储</p>
<p>​                    3.<strong>Vector</strong> :  <strong>线程安全，效率低</strong>，底层使用Object[ ] elementData存储</p>
<h4 id="2-ArrayList-源码分析"><a href="#2-ArrayList-源码分析" class="headerlink" title="2. ArrayList 源码分析"></a>2. ArrayList 源码分析</h4><h5 id="2-1-基于JDK7-情况下："><a href="#2-1-基于JDK7-情况下：" class="headerlink" title="2.1 基于JDK7 情况下："></a>2.1 基于JDK7 情况下：</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ArrayList  arraylist=<span class="keyword">new</span> ArrayList();<span class="comment">// 底层创建了一个长度是10的Object [ ] 数组 elementData</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">arrayList.add(<span class="number">134</span>); elementData[<span class="number">0</span>] =<span class="keyword">new</span> Integer(<span class="number">123</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">...</span></pre></td></tr><tr><td class="code"><pre><span class="line">arraylist.add(<span class="number">11</span>);<span class="comment">// 如果此次添加导致底层elementData数组容量不够，则扩容。默认的情况下，扩容到原来容量的1.5倍，同时需要将原有数组中的数据复制到新的数组中。</span></span></pre></td></tr></table></figure>

<h5 id="2-2-基于JDK8情况下："><a href="#2-2-基于JDK8情况下：" class="headerlink" title="2.2 基于JDK8情况下："></a>2.2 基于JDK8情况下：</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ArrayList  arraylist=<span class="keyword">new</span> ArrayList();<span class="comment">// 底层Object [ ] elementData的初始化为｛｝，并没有创建长度为10的数组</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">list.add(<span class="number">123</span>);<span class="comment">//第一次调用add 时，才创建长度为10的数组，并将123添加到elementData 中</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">...</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">//后续的添加和扩容操作与jdk7无异</span></span></pre></td></tr></table></figure>

<h5 id="2-3-小结"><a href="#2-3-小结" class="headerlink" title="2.3 小结"></a>2.3 小结</h5><p>​        jdk7 中的ArrayList 的对象的创建类似于单利模式的饿汉式，而JDK 8 中的ArrayList 的对象创建类似于 单例        模式的懒汉式，延迟了数组的创建，节省内存。</p>
<h4 id="3-LinkedList的源码分析"><a href="#3-LinkedList的源码分析" class="headerlink" title="3. LinkedList的源码分析"></a>3. LinkedList的源码分析</h4><p>​        LinkedList list=new LinkedList(); //内部声明了Node类型的first 属性和 last 属性(size:結合长度、firs:双向链表头部、last:双向链表尾部) ;</p>
<p>​        list.add(123); //将123封装到Node 中，创建了Node 对象。</p>
<p>​        其中，Node 的定义为: 体现了LinkedList的双向链表的说法        </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span>&lt;<span class="title">E</span>&gt; </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        E item;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Node&lt;E&gt; next;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Node&lt;E&gt; prev;</span></pre></td></tr><tr><td class="code"><pre><span class="line"> </span></pre></td></tr><tr><td class="code"><pre><span class="line">        Node(Node&lt;E&gt; prev, E element, Node&lt;E&gt; next) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">this</span>.item = element;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">this</span>.next = next;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">this</span>.prev = prev;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr></table></figure>

<h4 id="4-Vector"><a href="#4-Vector" class="headerlink" title="4. Vector"></a>4. Vector</h4><h4 id="5-迭代器与增强for循环"><a href="#5-迭代器与增强for循环" class="headerlink" title="5. 迭代器与增强for循环"></a>5. 迭代器与增强for循环</h4><h5 id="5-1-迭代器"><a href="#5-1-迭代器" class="headerlink" title="5.1 迭代器"></a>5.1 迭代器</h5><ul>
<li>GOF 给迭代器的定义为：提供一种方法访问一个容器对象中的各个元素，而又不需要暴露改对象的内部细节，迭代器模式，就是为容器而生；</li>
<li>如何是使用迭代器遍历集合？</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Collection&lt;Object&gt; collection = <span class="keyword">new</span> ArrayList();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        collection.add(<span class="string">"123"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        collection.add(<span class="string">"你好啊"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        collection.add(<span class="number">456</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(collection);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Iterator&lt;Object&gt; ite = collection.iterator();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    	<span class="comment">//hasNext();判断是否还有下一个元素</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">while</span> (ite.hasNext()) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="comment">//next();1.指针下移；2.将下移后集合位置上的返回</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            System.out.println(ite.next());</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr></table></figure>

<ul>
<li>Iterator 接口remove（）方法</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Collection&lt;Object&gt; collection = <span class="keyword">new</span> ArrayList();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        collection.add(<span class="string">"123"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        collection.add(<span class="string">"你好啊"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        collection.add(<span class="number">456</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(collection);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Iterator&lt;Object&gt; ite = collection.iterator();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">while</span> (ite.hasNext()) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            Object object=ite.next();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">if</span> (object.equals(<span class="number">456</span>)) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                ite.remove();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="comment">//System.out.println(ite.next());</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr></table></figure>

<ul>
<li>注意<ul>
<li>Iterator 可以删除集合元素，但是遍历过程中通过迭代器对象的remove()方法，不是集合的remove()方法</li>
<li><strong>如果还为调用next（）方法或在上一次调用next()方法之后已经调用了remove（）方法</strong>，再调用remove都会报lllegalStateException</li>
</ul>
</li>
</ul>
<h5 id="5-2-增强for循环-foreach"><a href="#5-2-增强for循环-foreach" class="headerlink" title="5.2 增强for循环(foreach)"></a>5.2 增强for循环(foreach)</h5><ul>
<li><p>增强for循环的作用： 简化迭代器的书写格式。(注意：增强for循环的底层还是使用了迭代器遍历。)</p>
</li>
<li><p>增强for循环的适用范围： 如果是实现了Iterable接口的对象或者是数组对象都可以使用增强for循环。</p>
</li>
<li><p>增强for循环的缺点：增强for循环和iterator遍历的效果是一样的，也就说增强for循环的内部也就是调用iteratoer实现的，但是增强for循环有些缺点，例如不能在增强循环里动态的删除集合内容、不能获取下标等。</p>
</li>
<li><p>增强for循环的格式：</p>
<p>for(数据类型 变量名 :遍历的目标){ //数据类型 变量名:声明一个变量用来接收遍历目标遍历后的元素</p>
<p>}</p>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Collection&lt;Object&gt; collection = <span class="keyword">new</span> ArrayList();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        collection.add(<span class="string">"123"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        collection.add(<span class="string">"你好啊"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        collection.add(<span class="number">456</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        System.out.println(collection);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">for</span>(Object objects: collection)&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            System.out.println(objects);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr></table></figure>

<h4 id="6-Collection集合与数组之间的转换"><a href="#6-Collection集合与数组之间的转换" class="headerlink" title="6.Collection集合与数组之间的转换"></a>6.Collection集合与数组之间的转换</h4><ul>
<li>集合——数组</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Collection&lt;Object&gt; collection = <span class="keyword">new</span> ArrayList();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        collection.add(<span class="string">"123"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        collection.add(<span class="string">"你好啊"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        collection.add(<span class="string">"456"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Object[] a = collection.toArray();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;a.length; i++) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            System.out.println(a[i]);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr></table></figure>

<ul>
<li>数组—–集合</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       Integer a[]= &#123;<span class="number">123</span>,<span class="number">345</span>,<span class="number">4433</span>&#125;;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       List list= Arrays.asList(a);</span></pre></td></tr><tr><td class="code"><pre><span class="line">       System.out.println(list);</span></pre></td></tr><tr><td class="code"><pre><span class="line">   &#125;</span></pre></td></tr></table></figure>

<h5 id="List-接口的常用方法（增、删、改、查、插、长度、遍历）"><a href="#List-接口的常用方法（增、删、改、查、插、长度、遍历）" class="headerlink" title="List 接口的常用方法（增、删、改、查、插、长度、遍历）"></a>List 接口的常用方法（增、删、改、查、插、长度、遍历）</h5><ul>
<li><p>add(Object object);</p>
</li>
<li><p>remove（int index）/remove(Object object);</p>
</li>
<li><p>set(int index,Object object);</p>
</li>
<li><p>get(int index);</p>
</li>
<li><p>add(int index,Object);</p>
</li>
<li><p>size();</p>
</li>
</ul>
<h3 id="四、Set-接口"><a href="#四、Set-接口" class="headerlink" title="四、Set 接口"></a>四、Set 接口</h3><h4 id="1-概述"><a href="#1-概述" class="headerlink" title="1. 概述"></a>1. 概述</h4><p>​    Java中Set接口继承自Collection接口，Set接口的实现类无序并且不可重复，这里的无序指的是添加的顺序和内部真实排列顺序无关。Set接口的实现类常用的有HashSet和TreeSet。</p>
<h4 id="2-HashSet"><a href="#2-HashSet" class="headerlink" title="2.HashSet"></a>2.HashSet</h4><h4 id="3-TreeSet"><a href="#3-TreeSet" class="headerlink" title="3.TreeSet"></a>3.TreeSet</h4><h4 id="4-LinkedHashSet"><a href="#4-LinkedHashSet" class="headerlink" title="4.LinkedHashSet"></a>4.LinkedHashSet</h4><h3 id="五、Map-接口"><a href="#五、Map-接口" class="headerlink" title="五、Map 接口"></a>五、Map 接口</h3><h4 id="1-Map"><a href="#1-Map" class="headerlink" title="1.Map"></a>1.Map</h4><h4 id="2-HashMap的底层实现原理"><a href="#2-HashMap的底层实现原理" class="headerlink" title="2. HashMap的底层实现原理"></a>2. HashMap的底层实现原理</h4><p>以JDK 7 为例：<br>HashMap  hashmap=new HashMap();<br>在实例化过后，底层创建一个长度16 的一维数组Entry[ ] table,<br>……<br>hasmap.put(key1,value1);<br>首先，调用key1,所在类的hasCode( ),计算key1哈希值，此哈希值经过某种计算以后，得到在Entry数组中存放的位置。</p>
<ol>
<li><p>如果此位置上的数据为空，此时的key1-value添加成功。（情况1）</p>
</li>
<li><p>如果此位置上的数据不为空，(意味着此位置上存在一个或多个数据（以链表形式存在），比较key1和已经存在的一个或多个数据的哈希值)：</p>
<ul>
<li>如果key1的哈希值与已经存在的数据哈希值都不相同，此时的key1-value1 添加成功；（情况2）</li>
<li>如果key1的哈希值和已经存在的某一个数据（key2-value2）的哈希值相同，继续比较：调用key1所在类的equals(key2)<ul>
<li>如果equals（）方法返回false, 此时key1-value1添加成功。（情况3）</li>
<li>如果equals（）方法返回true,使用value1 替换value2。</li>
</ul>
</li>
</ul>
<p>补充：关于情况2和情况3：此时key1-value1和原来的数据以链表的方式存储。<br>在不断添加的过程中，会涉及扩容的问题，默认的方式：扩容到原来的2倍，并原有数据复制过来</p>
<p>JDK 8相较于JDK 7在底层实现方面不同：</p>
<ol>
<li>new HashMap( ); 底层没有创建一个长度为16 的数组；</li>
<li>JDK 8底层数组是Node[ ] 而不是Entry[ ]</li>
<li>首次调用put( )方法时，创建长度16 的数组</li>
<li>JDK7 底层结构只有数组+链表，JDK8中底层结构：数组+链表+红黑树<br> 当数组索引位置的某一个位置上的元素以链表形式存在的数据个数 &gt; 8 且当前的数组的长度 &gt;64 时，此时此索引位置上的所有数据改为使用红黑树存储。</li>
</ol>
</li>
</ol>
<h4 id="3-LinkedHashMap的底层实现原理（了解）"><a href="#3-LinkedHashMap的底层实现原理（了解）" class="headerlink" title="3. LinkedHashMap的底层实现原理（了解）"></a>3. LinkedHashMap的底层实现原理（了解）</h4><h4 id="4-Map中定义的方法"><a href="#4-Map中定义的方法" class="headerlink" title="4. Map中定义的方法"></a>4. Map中定义的方法</h4><ul>
<li><p>添加、删除、修改操作：</p>
<p> Object put(key,value) ： 将指定的key-value 添加到或修改到当前map对象中；</p>
<p> void putAll(Map map ) : 将map 中的所有key-value对存放到当前map中；</p>
<p> Object remove(Object key) : 移除指定key的key-value对，并返回value;</p>
<p> void clear( ) : 清空当前map中的数据</p>
</li>
<li><p>元素查询操作：</p>
<p> Object get(Object key ) : 获取指定key对应的value;</p>
<p> boolean containsKey(Object key) : 是否包含指定的key;</p>
<p> boolean containsValue(Object value ) ：是否包含指定的value;</p>
<p> int size( ); 返回map 中key-value对的个数；</p>
<p> boolean isEmpty( ) : 判断当前map是否为空；</p>
<p> boolean equals(Object obj ) : 判断当前的map和参数对象obj是否相等；</p>
</li>
<li><p>元视图操作方法：</p>
<p>  Set keySet( ) : 返回所有key构成的集合 ；</p>
<p>  Collection values( ) : 返回所有value构成的Collection集合；</p>
<p>  Set entrySet( ) : 返回所有key-value对构成的集合</p>
</li>
</ul>
<h3 id="六、Collections-工具类使用"><a href="#六、Collections-工具类使用" class="headerlink" title="六、Collections 工具类使用"></a>六、Collections 工具类使用</h3><ol>
<li><p>Collections 是操作Set 、List、和Map 等集合的工具类；</p>
</li>
<li><p>常用方法：</p>
<ul>
<li>reverse(List); 反转list中元素的顺序</li>
<li>shuffle(List); 对List中元素随机排序</li>
<li>sort(List)；根据元素的自然顺序对指定的List集合按元素升序排序</li>
<li>sort(List,Comparator); 根据指定的Comparator产生的顺序对List集合进行排序</li>
<li>swap(List,int,int); 将指定的list集合中的i处元素和j处元素进行交换</li>
<li>Object max(Collection); 根据自然顺序，返回集合最大的元素</li>
<li>Object max(Collection,Comparator); 根据Comparator指定的顺序，返回集合中        最大的元素</li>
<li>Object min(Collection);</li>
<li>Object min(Collection,Comparator);</li>
<li>int frequency(Collection,Object); 返回集合中指定的元素的出现次数</li>
<li>void copy(List dest,List src); 将src 中的内容复制到dest中</li>
<li>boolean replaceAll(List list,Object oldVal,Object newVal);使用新值替换List对        象</li>
</ul>
</li>
<li><p>Collections中提供多个synchronizedXxx() 方法，该方法可使将指定集合包装成线程同步的集合，从而解决多线程并发访问集合时的线程安全</p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List list1=Collections.synchronizedList(list);</span></pre></td></tr></table></figure>


















]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>使用百度智能云实现文字、身份证识别等</title>
    <url>/2020/03/18/orc/</url>
    <content><![CDATA[<p> 根据公司业务需求，近期在做一个平台，其中需要对用户进行实名认证。在用户上传身份证之后自动识别身份证上的信息，然后提交相关的信息进行验证。</p>
<p>​        在网上搜索后了解到了百度智能云的文字识别，详细了解一下发现还不错，不足的是身份证等识别接口每天只有500次的免费额度，如果每天量比较大的多话就需要花钱了，由于前期需求量小，暂且接入此接口。</p>
<h3 id="一、准备步骤："><a href="#一、准备步骤：" class="headerlink" title="一、准备步骤："></a>一、准备步骤：<a id="more"></a></h3><ul>
<li><p>首先需要登录<a href="https://cloud.baidu.com/" target="_blank" rel="noopener">百度智能云</a>，进入控制台，找到文字识别，点击创建应用,填写相关信息；</p>
<p><img src="/2020/03/18/orc/1.png" alt></p>
<p><img src="/2020/03/18/orc/2.png" alt></p>
</li>
<li><p>创建完成之后，在管理应用中可以找到我们需要的 API Key 和Secret Key </p>
<p><img src="/2020/03/18/orc/3.png" alt></p>
</li>
</ul>
<h3 id="二、开发"><a href="#二、开发" class="headerlink" title="二、开发"></a>二、开发</h3><ul>
<li><p>首先我们 需要<strong>获取access_token</strong>  </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.baidu.ai.aip.auth;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.json.JSONObject;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.BufferedReader;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.InputStreamReader;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.net.HttpURLConnection;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.net.URL;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.List;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.Map;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment"> * 获取token类</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment"> */</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AuthService</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">/**</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">     * 获取权限token</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">     * <span class="doctag">@return</span> 返回示例：</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">     * &#123;</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">     * "access_token": "24.460da4889caad24cccdb1fea17221975.2592000.1491995545.282335-1234567",</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">     * "expires_in": 2592000</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">     * &#125;</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">     */</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">getAuth</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="comment">// 官网获取的 API Key 更新为你注册的</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        String clientId = <span class="string">"百度云应用的AK"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="comment">// 官网获取的 Secret Key 更新为你注册的</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        String clientSecret = <span class="string">"百度云应用的SK"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> getAuth(clientId, clientSecret);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">/**</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">     * 获取API访问token</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">     * 该token有一定的有效期，需要自行管理，当失效时需重新获取.</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">     * <span class="doctag">@param</span> ak - 百度云官网获取的 API Key</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">     * <span class="doctag">@param</span> sk - 百度云官网获取的 Securet Key</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">     * <span class="doctag">@return</span> assess_token 示例：</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">     * "24.460da4889caad24cccdb1fea17221975.2592000.1491995545.282335-1234567"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">     */</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">getAuth</span><span class="params">(String ak, String sk)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="comment">// 获取token地址</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        String authHost = <span class="string">"https://aip.baidubce.com/oauth/2.0/token?"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        String getAccessTokenUrl = authHost</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="comment">// 1. grant_type为固定参数</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                + <span class="string">"grant_type=client_credentials"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="comment">// 2. 官网获取的 API Key</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                + <span class="string">"&amp;client_id="</span> + ak</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="comment">// 3. 官网获取的 Secret Key</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                + <span class="string">"&amp;client_secret="</span> + sk;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            URL realUrl = <span class="keyword">new</span> URL(getAccessTokenUrl);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="comment">// 打开和URL之间的连接</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            HttpURLConnection connection = (HttpURLConnection) realUrl.openConnection();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            connection.setRequestMethod(<span class="string">"GET"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            connection.connect();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="comment">// 获取所有响应头字段</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            Map&lt;String, List&lt;String&gt;&gt; map = connection.getHeaderFields();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="comment">// 遍历所有的响应头字段</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">for</span> (String key : map.keySet()) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                System.err.println(key + <span class="string">"---&gt;"</span> + map.get(key));</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="comment">// 定义 BufferedReader输入流来读取URL的响应</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            BufferedReader in = <span class="keyword">new</span> BufferedReader(<span class="keyword">new</span> InputStreamReader(connection.getInputStream()));</span></pre></td></tr><tr><td class="code"><pre><span class="line">            String result = <span class="string">""</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            String line;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">while</span> ((line = in.readLine()) != <span class="keyword">null</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                result += line;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="comment">/**</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">             * 返回结果示例</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">             */</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            System.err.println(<span class="string">"result:"</span> + result);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            JSONObject jsonObject = <span class="keyword">new</span> JSONObject(result);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            String access_token = jsonObject.getString(<span class="string">"access_token"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">return</span> access_token;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            System.err.printf(<span class="string">"获取token失败！"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            e.printStackTrace(System.err);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<ul>
<li>获取token 之后就可以进行下一步操作了，进行身份证的识别</li>
</ul>
</li>
</ul>
<p><img src="/2020/03/18/orc/4.png" alt></p>
<p><img src="/2020/03/18/orc/5.png" alt></p>
<p>代码示例:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.baidu.ai.aip;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> com.baidu.ai.aip.utils.Base64Util;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> com.baidu.ai.aip.utils.FileUtil;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> com.baidu.ai.aip.utils.HttpUtil;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.net.URLEncoder;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">* 身份证识别</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">*/</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Idcard</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">/**</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">    * 重要提示代码中所需工具类</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">    * FileUtil,Base64Util,HttpUtil,GsonUtils请从</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">    * https://ai.baidu.com/file/658A35ABAB2D404FBF903F64D47C1F72</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">    * https://ai.baidu.com/file/C8D81F3301E24D2892968F09AE1AD6E2</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">    * https://ai.baidu.com/file/544D677F5D4E4F17B4122FBD60DB82B3</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">    * https://ai.baidu.com/file/470B3ACCA3FE43788B5A963BF0B625F3</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">    * 下载</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">    */</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">idcard</span><span class="params">()</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="comment">// 请求url</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        String url = <span class="string">"https://aip.baidubce.com/rest/2.0/ocr/v1/idcard"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">try</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="comment">// 本地文件路径</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            String filePath = <span class="string">"[本地文件路径]"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">byte</span>[] imgData = FileUtil.readFileByBytes(filePath);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            String imgStr = Base64Util.encode(imgData);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            String imgParam = URLEncoder.encode(imgStr, <span class="string">"UTF-8"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">            String param = <span class="string">"id_card_side="</span> + <span class="string">"front"</span> + <span class="string">"&amp;image="</span> + imgParam;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="comment">// 注意这里仅为了简化编码每一次请求都去获取access_token，线上环境access_token有过期时间， 客户端可自行缓存，过期后重新获取。</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            String accessToken = <span class="string">"[调用鉴权接口获取的token]"</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">            String result = HttpUtil.post(url, accessToken, param);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            System.out.println(result);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">return</span> result;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            e.printStackTrace();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        Idcard.idcard();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>注意： </p>
<ol>
<li>图像数据，base64编码后进行urlencode ；</li>
<li>有的base64编码后有头部“Base64：”要去掉</li>
</ol>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>解决小程序 app.js中的onLaunch中的数据 在page的onLoade中接收不到的问题</title>
    <url>/2020/03/09/xiaochegnxu/</url>
    <content><![CDATA[<h3 id="一、问题阐述"><a href="#一、问题阐述" class="headerlink" title="一、问题阐述"></a>一、问题阐述</h3><p>​      由于工作的安排，最近在改我们公司的一个小程序，在此期间碰到了一个坑，遇到的问题是，实现登录时，app.js中 onLaunch 实现数据请求拿到后端返回的参数后写入 Storage ，然后在首页 Storage 取信息，但是并未能取到这个参数。调试后发现  app.js onLaunch并没有先于onLoad 执行完再执行，而是onLoad先执行完 ，所以进入首页未能取到相关参数。<a id="more"></a>跟踪了一下问题发现，app.js onLaunch  中登录的请求时异步的，这就可能导致请求的参数在page onLoade 执行完后才会返回。</p>
<h3 id="二、解决办法"><a href="#二、解决办法" class="headerlink" title="二、解决办法"></a>二、解决办法</h3><h3 id="1-第一种方法：定义一个回调函数-。"><a href="#1-第一种方法：定义一个回调函数-。" class="headerlink" title="1 . 第一种方法：定义一个回调函数 。"></a>1 . 第一种方法：定义一个回调函数 。</h3><p>​       Page页面判断一下当前app.globalData.employId是否有值，如果没有（第一次）则定义定义一个app方法（回调函数）app.employIdCallback = employId =&gt; {…}。App页面在请求success后判断时候有Page页面定义的回调方法，如果有就执行该方法 ，实现代码：</p>
<p>​        app.js </p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">reLogin: <span class="function"><span class="keyword">function</span> (<span class="params">app</span>) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="comment">// 执行登录，获取用户身份标识openid</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="built_in">console</span>.log(<span class="string">"2.现在执行到 reLogin"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="keyword">var</span> that = <span class="keyword">this</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="comment">//return new Promise((resolve, reject) =&gt; &#123;</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">       swan.login(&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">           success: <span class="function"><span class="keyword">function</span> (<span class="params">res</span>) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">               <span class="built_in">console</span>.log(res);</span></pre></td></tr><tr><td class="code"><pre><span class="line">               <span class="built_in">console</span>.log(<span class="string">"3.现在执行到  swan.login"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">               swan.request(&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                   url: that.globalData.loginUrl,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                   data: &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                       act: <span class="string">'BDXCXlogin'</span>,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                       code: res.code,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                   &#125;,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                   success: <span class="function"><span class="keyword">function</span> (<span class="params">res</span>) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                       <span class="comment">// swan.setStorageSync("sessionid", res.header["Set-Cookie"]);</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                       <span class="built_in">console</span>.log(<span class="string">"login check statusCode:"</span> + res.statusCode);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                       <span class="built_in">console</span>.log(<span class="string">"4.现在请求后台登录"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                       <span class="built_in">console</span>.log(res.data);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                       <span class="keyword">if</span> (res.statusCode == <span class="number">200</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                           <span class="keyword">var</span> result = <span class="built_in">JSON</span>.parse(res.data);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                          <span class="comment">// var result = res.data;</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                           <span class="built_in">console</span>.log(<span class="string">"result的val："</span> + result);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                           <span class="built_in">console</span>.log(<span class="string">"result.retcode的值："</span> + result.retcode);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                           <span class="keyword">if</span> (result.retcode == <span class="string">'SUCCESS'</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                               swan.setStorage(&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                   key: <span class="string">""</span> + that.globalData.tokenName + <span class="string">""</span>,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                   data: <span class="string">""</span> + result.token + <span class="string">""</span>,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                   success: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                       <span class="built_in">console</span>.log(<span class="string">"登录成功后将token存储完成：storage token success"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                   &#125;,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                   fail: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                       <span class="built_in">console</span>.log(<span class="string">"storage token failed"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                   &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                               &#125;)</span></pre></td></tr><tr><td class="code"><pre><span class="line">                               <span class="built_in">console</span>.log(<span class="string">"当前token"</span> + result.token);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                               <span class="built_in">console</span>.log(<span class="string">"现在执行 resolve1"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                               <span class="comment">//resolve(result.token);</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                               <span class="keyword">if</span> (that.employIdCallback) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                   that.employIdCallback(result.token);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                               &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                           &#125; <span class="keyword">else</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                               <span class="built_in">console</span>.log(<span class="string">"login check failed:"</span> + result.retmsg);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                               <span class="built_in">console</span>.log(<span class="string">"现在执行 reject1"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                               <span class="comment">// reject(result.retmsg);</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                           &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                       &#125; <span class="keyword">else</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                           <span class="built_in">console</span>.log(<span class="string">"login request network error:"</span> + res.statusCode);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                           swan.showToast(&#123; <span class="attr">icon</span>: <span class="string">'none'</span>, <span class="attr">title</span>: <span class="string">'login request network error:'</span> + res.statusCode + <span class="string">''</span> &#125;)</span></pre></td></tr><tr><td class="code"><pre><span class="line">                           <span class="built_in">console</span>.log(<span class="string">"现在执行 reject2"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                           <span class="comment">//  reject(res.statusCode);</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                       &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">                       <span class="comment">//resolve(result.token);</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                   &#125;,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                   fail: <span class="function"><span class="keyword">function</span> (<span class="params">res</span>) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                       <span class="built_in">console</span>.log(<span class="string">"现在执行 reject3"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                       <span class="comment">// reject(res);</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                       <span class="built_in">console</span>.log(res);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                       <span class="built_in">console</span>.log(<span class="string">"login request failed"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                       swan.showToast(&#123; <span class="attr">icon</span>: <span class="string">'none'</span>, <span class="attr">title</span>: <span class="string">'login request failed'</span> &#125;)</span></pre></td></tr><tr><td class="code"><pre><span class="line">                   &#125;,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                   complete: <span class="function"><span class="keyword">function</span> (<span class="params">res</span>) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                       <span class="built_in">console</span>.log(<span class="string">'login request completed'</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                       swan.hideLoading();</span></pre></td></tr><tr><td class="code"><pre><span class="line">                       swan.hideNavigationBarLoading();</span></pre></td></tr><tr><td class="code"><pre><span class="line">                   &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">               &#125;);</span></pre></td></tr><tr><td class="code"><pre><span class="line">           &#125;,</span></pre></td></tr><tr><td class="code"><pre><span class="line">           fail: <span class="function"><span class="keyword">function</span> (<span class="params">err</span>) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">               <span class="built_in">console</span>.log(<span class="string">'swan login fail'</span>, err);</span></pre></td></tr><tr><td class="code"><pre><span class="line">           &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       &#125;);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="comment">// &#125;)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">   &#125;</span></pre></td></tr></table></figure>

<p>​          indx.js</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">onLoad(query) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="comment">// 页面加载 </span></span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="keyword">var</span> that = <span class="keyword">this</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="built_in">console</span>.log(<span class="string">"现在进入webview.js"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="built_in">console</span>.info(<span class="string">`Page onLoad with query: <span class="subst">$&#123;<span class="built_in">JSON</span>.stringify(query)&#125;</span>`</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="built_in">console</span>.info(query);</span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="keyword">var</span> token = swan.getStorageSync(app.globalData.tokenName);</span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="keyword">if</span> (token.length &gt; <span class="number">0</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="built_in">console</span>.log(<span class="string">"webview.js  getStorageSync 获取  token&gt;&gt;"</span> + token);</span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="keyword">this</span>.setData(&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">               weburl: <span class="string">''</span> + app.globalData.weburl + <span class="string">'?tokenData='</span> + token + <span class="string">''</span>,</span></pre></td></tr><tr><td class="code"><pre><span class="line">           &#125;)</span></pre></td></tr><tr><td class="code"><pre><span class="line">       &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       <span class="keyword">else</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="comment">// app.reLogin().then((res) =&gt; &#123;</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="comment">//     var token = swan.getStorageSync(app.globalData.tokenName);</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="comment">//     console.log('现在执行app.reLogin().then' +token);</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="comment">//     console.log('当前res数据:' + res);</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="comment">//     console.log('token2' + res);</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="comment">//     this.setData(&#123;</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="comment">//         weburl: '' + app.globalData.weburl + '?tokenData=' + res + '',</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="comment">//     &#125;)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="comment">// &#125;, (error) =&gt; &#123;</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="comment">//     console.log('失败：' + error)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="comment">// &#125;)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">           <span class="comment">//为了解决小程序数据请求异步问题 ，app.js onLaunch 中数据请求的数据可能在paages  onLoad 之后才返回 </span></span></pre></td></tr><tr><td class="code"><pre><span class="line">           app.employIdCallback = <span class="function"><span class="params">account</span> =&gt;</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">               <span class="keyword">var</span> token = swan.getStorageSync(app.globalData.tokenName);</span></pre></td></tr><tr><td class="code"><pre><span class="line">               <span class="built_in">console</span>.log(<span class="string">'现在执行app.reLogin().then'</span> + token);</span></pre></td></tr><tr><td class="code"><pre><span class="line">               <span class="built_in">console</span>.log(<span class="string">'当前res数据:'</span> + account);</span></pre></td></tr><tr><td class="code"><pre><span class="line">               <span class="built_in">console</span>.log(<span class="string">'token2'</span> + token);</span></pre></td></tr><tr><td class="code"><pre><span class="line">               <span class="keyword">this</span>.setData(&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                   weburl: <span class="string">''</span> + app.globalData.weburl + <span class="string">'?tokenData='</span> + token + <span class="string">''</span>,</span></pre></td></tr><tr><td class="code"><pre><span class="line">               &#125;)</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">           &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">       &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">   &#125;,</span></pre></td></tr></table></figure>

<h3 id="2-第二种方法-：-使用promise，判断进程状态，在index中去判断进程状态，再去执行页面的加载"><a href="#2-第二种方法-：-使用promise，判断进程状态，在index中去判断进程状态，再去执行页面的加载" class="headerlink" title="2. 第二种方法  ：  使用promise，判断进程状态，在index中去判断进程状态，再去执行页面的加载"></a>2. 第二种方法  ：  使用promise，判断进程状态，在index中去判断进程状态，再去执行页面的加载</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> promise = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span>(<span class="params">resolve, reject</span>) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"> <span class="keyword">if</span> (<span class="comment">/* 异步操作成功 */</span>)&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"> resolve(value);</span></pre></td></tr><tr><td class="code"><pre><span class="line"> &#125; <span class="keyword">else</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"> reject(error);</span></pre></td></tr><tr><td class="code"><pre><span class="line"> &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">promise.then(<span class="function"><span class="keyword">function</span>(<span class="params">value</span>) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"> <span class="comment">// success</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;, <span class="function"><span class="keyword">function</span>(<span class="params">value</span>) </span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"> <span class="comment">// failure</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;);</span></pre></td></tr></table></figure>

<p>​     Promise 构造函数接受一个函数作为参数，该函数的两个参数分别是 resolve 方法和 reject 方法。</p>
<p>​    如果异步操作成功，则用 resolve 方法将 Promise 对象的状态，从「未完成」变为「成功」（即从 pending 变为 resolved）；</p>
<p>​    如果异步操作失败，则用 reject 方法将 Promise 对象的状态，从「未完成」变为「失败」（即从 pending 变为 rejected）。</p>
<p><strong>具体实现的代码见第一种方法中的注释部分</strong></p>
]]></content>
      <categories>
        <category>踩坑</category>
      </categories>
      <tags>
        <tag>小程序</tag>
      </tags>
  </entry>
  <entry>
    <title>手把手教你搭建hexo个人博客</title>
    <url>/2019/11/30/hello-world/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>​     前段时间，心血来潮，想搭建一个个人博客网站，对于一个程序员来书，拥有一个博客是一个非常必要的事，紧接着我在网上了解到了hexo 。用hexo 来搭建一个个人博客是非常便捷的，仅需要我们进行相关的配置，即可拥有一个高逼格的个人博客了。<a id="more"></a></p>
<p>废话不多说，我们开始！</p>
<h2 id="步骤"><a href="#步骤" class="headerlink" title="步骤"></a>步骤</h2><ul>
<li>安装Git</li>
<li>安装node.js</li>
<li>安装hexo</li>
<li>更换主题</li>
<li>发布文章</li>
<li>部署到github</li>
</ul>
<h2 id="1-安装Git"><a href="#1-安装Git" class="headerlink" title="1.安装Git"></a>1.安装Git</h2><p>相信大家对于Git 都并不陌生，网上有很多关于Git 的教程和安装说明，这里就不过多的赘述，推荐廖雪峰的一篇Git<a href="https://www.liaoxuefeng.com/wiki/896043488029600/896067074338496" target="_blank" rel="noopener">教程</a>。</p>
<h2 id="2-安装node-js"><a href="#2-安装node-js" class="headerlink" title="2.安装node.js"></a>2.安装node.js</h2><h2 id="3-安装hexo"><a href="#3-安装hexo" class="headerlink" title="3.安装hexo"></a>3.安装hexo</h2><p>安装好Git与node.js之后，新建文件夹blog(任意名即可) 之后通过cd 进入此文件夹（或者在此文件夹右键git bash）,输入命令：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">npm install -g hexo-cli</span></pre></td></tr></table></figure>

<p>  安装结束后可以使用hexo -v 查看版本，接下来输入：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo init myblog</span></pre></td></tr></table></figure>

<p>进行初始化操作（myblog任意名都行，根据自己需求）</p>
<p>紧接着 cd  myblog 进入这个文件夹</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install  &#x2F;&#x2F;安装所有依赖</span></pre></td></tr></table></figure>

<p>此时我们hexo 安装完成，指定的文件夹目录：</p>
<ul>
<li>node_modules: 依赖包</li>
<li>public：存放生成的页面</li>
<li>scaffolds：生成文章的一些模板</li>
<li>source：用来存放你的文章</li>
<li>themes：主题</li>
<li>** _config.yml: 博客的配置文件**</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo g</span></pre></td></tr><tr><td class="code"><pre><span class="line">hexo s</span></pre></td></tr></table></figure>

<p>打开hexo 服务，在浏览器输入 localhost:4000 就查看你的博客了。</p>
<h2 id="4-更换主题"><a href="#4-更换主题" class="headerlink" title="4. 更换主题"></a>4. 更换主题</h2><p>这时我们会发现，刚开始的博客并不是非常的好看，但是有很多的主题供我们选择，这里我比较推荐next 这个主题，并且网上的教程很多。<a href="http://theme-next.iissnan.com/" target="_blank" rel="noopener">使用文档</a>。</p>
<h3 id="安装next"><a href="#安装next" class="headerlink" title="安装next"></a>安装next</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ cd your-hexo-site</span></pre></td></tr><tr><td class="code"><pre><span class="line">$ git clone https:&#x2F;&#x2F;github.com&#x2F;iissnan&#x2F;hexo-theme-next themes&#x2F;next</span></pre></td></tr></table></figure>

<h3 id="启用主题"><a href="#启用主题" class="headerlink" title="启用主题"></a>启用主题</h3><p> 与所有 Hexo 主题启用的模式一样。 当 克隆/下载 完成后，打开 <strong>站点配置文件</strong>， 找到 <code>theme</code> 字段，并将其值更改为 <code>next</code> </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">theme: next</span></pre></td></tr></table></figure>

<p>此时，我们的个人博客得主题就变成next 了，更多的配置以及使用方法都在使用文档中以及网上各种美化教程，我们都可以慢慢摸索。这里推荐一两个我看过的教程：<a href="https://blog.csdn.net/nightmare_dimple/article/details/86661502" target="_blank" rel="noopener">Hexo博客优化之Next主题美化</a>、<a href="https://blog.csdn.net/qq_31279347/article/details/82427562" target="_blank" rel="noopener">Hexo Next主题进阶详细教程</a>。后期也会写一些相关的教程。</p>
<h2 id="5-发布文章"><a href="#5-发布文章" class="headerlink" title="5.发布文章"></a>5.发布文章</h2><p>输入：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo new title 你的文章名字</span></pre></td></tr></table></figure>

<p>此时就会在\myblog\source_posts中新建一个.md文件，打开此文件在里面输入你想要发表的内容（makedown语法）。</p>
<h2 id="6-部署到GitHub"><a href="#6-部署到GitHub" class="headerlink" title="6.部署到GitHub"></a>6.部署到GitHub</h2><h3 id="创建个人仓库"><a href="#创建个人仓库" class="headerlink" title="创建个人仓库"></a>创建个人仓库</h3><p>GitHub.com中看到一个New repository，新建仓库 </p>
<p>创建一个和你用户名相同的仓库，<a href="http://xn--yfr16an19l.github.io/" target="_blank" rel="noopener">后面加.github.io</a>，只有这样，将来要部署到GitHub page的时候，才会被识别，<a href="http://xn--xxxx-4m5f354ev5p.github.io/" target="_blank" rel="noopener">也就是xxxx.github.io</a>，其中xxx就是你注册GitHub的用户名 , 点击create repository .</p>
<p>在你博客个根目录，右键git bash:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git config --global user.name &quot;yourname&quot;&#x2F;&#x2F;GitHub用户名</span></pre></td></tr><tr><td class="code"><pre><span class="line">git config --global user.email &quot;youremail&quot;&#x2F;&#x2F; GitHub的邮箱</span></pre></td></tr></table></figure>

<p>检验输入结果:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git config user.name</span></pre></td></tr><tr><td class="code"><pre><span class="line">git config user.email</span></pre></td></tr></table></figure>

<p> 然后创建SSH ：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t rsa -C &quot;youremail&quot;</span></pre></td></tr></table></figure>

<p> 这个时候它会告诉你已经生成了.ssh的文件夹。在你的电脑中找到这个文件夹。 其中，id_rsa是你这台电脑的私人秘钥，id_rsa.pub是公共秘钥,把这个公钥放在GitHub上，而后在GitHub的setting中，找到SSH keys的设置选项，点击New SSH key,把你的id_rsa.pub里面的信息复制进去。</p>
<p> 查看是否成功 :</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ssh -T git@github.com</span></pre></td></tr></table></figure>

<p>这个时候我们就可以部署在github上了，打开 站点配置文件 <code>_config.yml</code> ，找到 deploy ，并修改为：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">deploy:</span></pre></td></tr><tr><td class="code"><pre><span class="line">  type: git</span></pre></td></tr><tr><td class="code"><pre><span class="line">  repo: https:&#x2F;&#x2F;github.com&#x2F;YourgithubName&#x2F;YourgithubName.github.io.git</span></pre></td></tr><tr><td class="code"><pre><span class="line">  branch: master</span></pre></td></tr></table></figure>

<p>然后安装 deploy-git ，</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span></pre></td></tr></table></figure>

<p>最后：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo clean</span></pre></td></tr><tr><td class="code"><pre><span class="line">hexo generate</span></pre></td></tr><tr><td class="code"><pre><span class="line">hexo deploy</span></pre></td></tr></table></figure>

<p>这是我们就就可以通过 <a href="http://yourname.github.io" target="_blank" rel="noopener">http://yourname.github.io</a> 来访问你的个人博客了。</p>
]]></content>
      <categories>
        <category>hexo</category>
      </categories>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>HEXO Next 主题 添加在线联系功能</title>
    <url>/2019/11/17/page/</url>
    <content><![CDATA[<p>在我们使用hexo 搭建我们个人博客时，我们可以使用DaoVoice 为我们的博客添加在线联系的功能。<br>效果：</p>
<a id="more"></a>
<p><img src="https://img-blog.csdnimg.cn/20191117211422673.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3cxMzc2ODM0NTg5,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<h3 id="注册"><a href="#注册" class="headerlink" title="注册"></a>注册</h3><p>首先我们需要进行注册，<a href="http://www.daovoice.io/" target="_blank" rel="noopener">点击前往注册</a>，注册成功之后进入控制台，应用设置&gt;安装到网站,获取我们所需要的app_id：<br><img src="https://img-blog.csdnimg.cn/20191117205343685.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3cxMzc2ODM0NTg5,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<h3 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h3><p>接下我们需要在主题中进行一些配置，这边使用的是next主题，就以next主题为例：<br>打开文件\themes\next\layout_partials\head\head.swig,任意位置插入以下代码：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;% <span class="keyword">if</span> theme.daovoice %&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &lt;script&gt;</span></pre></td></tr><tr><td class="code"><pre><span class="line">  (<span class="function"><span class="keyword">function</span>(<span class="params">i,s,o,g,r,a,m</span>)</span>&#123;i[<span class="string">"DaoVoiceObject"</span>]=r;i[r]=i[r]||<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;(i[r].q=i[r].q||[]).push(<span class="built_in">arguments</span>)&#125;,i[r].l=<span class="number">1</span>*<span class="keyword">new</span> <span class="built_in">Date</span>();a=s.createElement(o),m=s.getElementsByTagName(o)[<span class="number">0</span>];a.async=<span class="number">1</span>;a.src=g;a.charset=<span class="string">"utf-8"</span>;m.parentNode.insertBefore(a,m)&#125;)(<span class="built_in">window</span>,<span class="built_in">document</span>,<span class="string">"script"</span>,(<span class="string">'https:'</span> == <span class="built_in">document</span>.location.protocol ? <span class="string">'https:'</span> : <span class="string">'http:'</span>) + <span class="string">"//widget.daovoice.io/widget/你的app_id.js"</span>,<span class="string">"daovoice"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">  daovoice(<span class="string">'init'</span>, &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">      app_id: <span class="string">"&#123;&#123;theme.daovoice_app_id&#125;&#125;"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;);</span></pre></td></tr><tr><td class="code"><pre><span class="line">  daovoice(<span class="string">'update'</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">  &lt;<span class="regexp">/script&gt;</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="regexp">&#123;% endif %&#125;</span></span></pre></td></tr></table></figure>
<p>在主题配置文件中插入以下代码：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"># Online contact </span></pre></td></tr><tr><td class="code"><pre><span class="line">daovoice: <span class="literal">true</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">daovoice_app_id: 这里输入前面获取的app_id</span></pre></td></tr></table></figure>
<p>以上就是我们需要配置的内容，配置完成之后，我们进行hexo clean,hexo s 即可查看效果。不过此时我们会发现，DaoVoice 的图标会与我们博客上的按钮重合，影响使用，这时候我们可以在“聊天设置”中进行设置：<br><img src="https://img-blog.csdnimg.cn/20191117210753372.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3cxMzc2ODM0NTg5,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><img src="https://img-blog.csdnimg.cn/20191117210811553.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3cxMzc2ODM0NTg5,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>我们通过hexo s 一边修改一遍测试直到我们满意的效果。</p>
]]></content>
      <categories>
        <category>hexo</category>
      </categories>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
</search>
